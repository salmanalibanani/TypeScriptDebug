{
  "name": "app",
  "version": "1.0.0",
  "description": "Simplest setup to debug TypeScript projects",
  "scripts": {
    "build": "tsc --noEmit",
    "build:w": "tsc --noEmit --watch"
  },
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=7.0.0"
  },
  "devDependencies": {
    "ts-node": "^10.4.0",
    "tsconfig-paths": "^3.12.0",
    "typescript": "^4.5.2"
  },
  "dependencies": {}
}
